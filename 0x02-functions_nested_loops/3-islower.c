#include "main.h"

/**
 * _islower - Return 1 if letter is lowercase, 0 if not.
 * @char1: char type letter
 * Return: 1 if lowercase, 0 if not lowercase
 */

int _islower(int char1)

{

if (char1 > 96 && char1 < 123)

return (1);

else

return (0);
}
